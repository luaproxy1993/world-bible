# {{SLUG}} · Core rule book

JSON is canonical. This folder is a core rule book, not a campaign and
not a picture pack.

```
lock.json
world/world.json
play/play.json
stage/stage.json
kit/kit.json
timeline/timeline.json
art/art.json
play-seeds.json
```

Pictures live in `pack/` and are written by `/world-bible-pack` in a later
session. A plotted sitting is `/rpg-campaign-maker`.

Compile writes `overview.html` and `WORLD_BIBLE.md`.
